# 云函数部署说明

## 功能说明
本云函数用于处理AI面试系统的核心功能，包括面试流程管理、AI问答生成、参考思路生成和总结报告生成。

## 部署要求
1. 云函数需要正确部署到云端
2. 需要配置必要的环境变量
3. 云函数依赖需要正确安装

## 解决步骤

### 1. 部署云函数
1. 打开微信开发者工具
2. 在左侧文件树中找到 `cloudfunctions/interview` 文件夹
3. 右键点击 `interview` 文件夹
4. 选择"上传并部署：云端安装依赖"
5. 等待部署完成

### 2. 配置环境变量
1. 登录微信云开发控制台：https://console.cloud.tencent.com/tcb
2. 选择你的云环境：`cloudbase-4ga21u2n9c7146e0`
3. 进入"云函数" → "interview" → "函数配置"
4. 添加环境变量：
   - 变量名：`ZHIPUAI_API_KEY`
   - 变量值：你的智谱AI API密钥

### 3. 验证部署
1. 在微信开发者工具的控制台中运行测试脚本：
```javascript
// 测试云函数是否正常工作
wx.cloud.callFunction({
  name: 'interview',
  data: { action: 'health' }
}).then(res => {
  console.log('健康检查结果:', res);
}).catch(err => {
  console.error('健康检查失败:', err);
});
```

### 4. 常见问题
- **云函数调用失败**：检查云函数是否已部署
- **API密钥错误**：确认环境变量已正确配置
- **依赖缺失**：重新部署云函数，选择"云端安装依赖"
- **超时问题**：确保云函数平台超时时间设置为10000ms以上

## 测试步骤
1. 完成一次面试
2. 进入总结页面
3. 点击任意问题的"点击获取参考思路"
4. 应该显示"生成中..."状态
5. 等待几秒钟后显示参考思路内容

## 性能优化
- 参考思路生成使用快速模型（glm-4-flash）
- 超时时间优化：快速生成2.5秒，完整生成8秒
- 支持轮询机制，确保生成完成

## 注意事项
- 确保微信开发者工具版本支持云开发
- 确保小程序已开通云开发功能
- 智谱AI API密钥需要有效且有足够额度
